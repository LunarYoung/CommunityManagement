<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zqg.communityManagementBackend.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.zqg.communityManagementBackend.entity.User">

        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>

    </resultMap>

    <select id="getList"  resultMap="BaseResultMap">

        select * from user
    </select>

    <update id="updateUser" parameterType="com.zqg.communityManagementBackend.entity.User">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        update user
        <set>
            <if test="sex != null">
                sex = #{sex,jdbcType=VARCHAR},
            </if>
            <if test="grade != null">
                grade = #{grade,jdbcType=INTEGER},
            </if>
            <if test="depart != null">
                depart = #{depart,jdbcType=VARCHAR},
            </if>
            <if test="address != null">
                address = #{address,jdbcType=VARCHAR},
            </if>
            <if test="phone != null">
                phone = #{phone,jdbcType=INTEGER},
            </if>

        </set>
        where user_app_id = #{userAppId,jdbcType=INTEGER}
    </update>

    <insert id="insertAppId" >
         insert IGNORE into user(user_app_id)
        values(
                  #{appId,jdbcType=VARCHAR}
              )
    </insert>

</mapper>
