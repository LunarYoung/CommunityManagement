<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zqg.communityManagementBackend.mapper.AppAppletsMapper">
    <resultMap id="BaseResultMap" type="com.zqg.communityManagementBackend.entity.Applets">

        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>

    </resultMap>

    <update id="updateApplets" parameterType="com.zqg.communityManagementBackend.entity.Applets">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        update applets
        <set>
            <if test="img1 != null">
                img1 = #{img1,jdbcType=VARCHAR},
            </if>
            <if test="img2 != null">
                img2 = #{img2,jdbcType=VARCHAR},
            </if>
            <if test="img3 != null">
                img3 = #{img3,jdbcType=VARCHAR},
            </if>
            <if test="status != null">
                status = #{status,jdbcType=VARCHAR},
            </if>

        </set>
        where onlyC = 1
    </update>

    <select id="selectApplets" resultType="com.zqg.communityManagementBackend.entity.Applets" >
        select
           *
        from applets
        where onlyC = 1
    </select>

    <select id="selectMyCom" parameterType="String" resultType="com.zqg.communityManagementBackend.entity.Member" >
        select
            *
        from member
        where open_id =#{openId}
    </select>

    <select id="selectManage" resultType="com.zqg.communityManagementBackend.entity.Manage" >
        select
            *
        from manage
    </select>

    <insert id="addManage" parameterType="com.zqg.communityManagementBackend.entity.Manage">
        insert into manage(name,password) values(#{password},#{name})
    </insert>

    <select id="confirmManage"   parameterType="java.lang.String" resultType="java.lang.String" >
        select
            password
        from manage
         where name = #{name}
    </select>
    <update id="updateManage">
        update applets
        <set>

             name = #{name,jdbcType=VARCHAR}

        </set>
        where  onlyC = 1
    </update>

    <delete id="deleteManage" parameterType="java.lang.String">
        delete from manage
        where name = #{name}
    </delete>
    <select id="selectMyApply" parameterType="String" resultType="com.zqg.communityManagementBackend.entity.AllApply" >
        select
            *
        from all_apply
        where open_id =#{openId}
    </select>


    <update id="updateMyCom" parameterType="com.zqg.communityManagementBackend.entity.MyCom">
        update my_com
        <set>
            <if test="position != null">
                position = #{position,jdbcType=VARCHAR},
            </if>
        </set>
        where association = #{association}  and open_id = #{openId}
    </update>

    <insert id="addPublic" parameterType="com.zqg.communityManagementBackend.entity.Public">
        insert into public_apply(item,name,sex,photo,depart,grade,time) values(#{item},#{name},#{sex},#{photo},#{depart},#{grade},#{time})
    </insert>

    <update id="sign" >
        update notice
        <set>
            sign = '已签到'
        </set>

        where id = #{id}
    </update>

</mapper>
